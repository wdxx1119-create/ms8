name: Contract diagnostics

on:
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  locate-contracts:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"

      - name: Capture first pytest failure
        env:
          PYTHONPATH: src
        run: |
          set +e
          python -m pytest tests/ -q -x > pytest-failure.txt 2>&1
          code=$?
          echo "pytest_exit_code=$code" >> pytest-failure.txt
          exit 0

      - name: Upload diagnostics
        uses: actions/upload-artifact@v4
        with:
          name: contract-diagnostics
          path: pytest-failure.txt
          if-no-files-found: error
          retention-days: 1
